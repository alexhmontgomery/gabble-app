<!DOCTYPE html>
<html>
  <head>
    <meta charset="utf-8">
    <title>Gabble | Home</title>
    <link rel="stylesheet" href="/css/master.css">
    <link rel="icon" type="image/png" href="/images/gabble-logo1.png">
    <link href="https://fonts.googleapis.com/css?family=Quicksand|Open+Sans" rel="stylesheet">
    <meta name="viewport" content="width=device-width, initial-scale=1">

  </head>
  <body>
    <header>
      <div class="head-title">
        <div class="logo-area">
          <img class="head-logo" src="/images/gabble-logo1.png" alt="gabble logo">
        </div>
        <div class="title-area">
          <h1>Gabble</h1>
        </div>
      </div>
      <div class="head-links">
        <a href="/">Feed</a>
        <a href="/profile/{{user}}">Logged in as:<br>{{user}}</a>
        <a href="/logout">Logout</a>
      </div>
    </header>

    <main class="home-main">
      <div class="home-create-gab">
        <form class="create-gab" action="/createAGab" method="post">
          <label for="content">Create a new Gab:</label>
          <button type="submit" name="gabCreate">CREATE</button>
        </form>
      </div>
      <div class="home-gab-displays">
        {{#gabs}}
        <div class="gab-post" id="gab-{{id}}">
          <a href="/profile/{{username}}"><h4>{{username}}</h4></a>
          <a href="/gab/{{id}}"><p>{{content}}</p>
          <h5>Posted: {{createdAt}}</h5>
          <h5>Likes: {{like.length}}</h5></a>
          <form class="like-button" action="/like" method="post">
            <button type="submit" name="gabnum" value={{id}}>Like</button>
          </form>
        </div>
        {{/gabs}}
      </div>
    </main>
  </body>
</html>
